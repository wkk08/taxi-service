# 🚕 Система такси (Taxi Service)

Современная система такси на основе Flask + Docker + Kubernetes + GitHub Actions с полным CI/CD конвейером и возможностью сине-зеленого развертывания.

## 📋 Обзор проекта

Это полная серверная система такси, реализующая основные функции управления пользователями, бронирования поездок, диспетчеризации водителей, обработки платежей и т.д. Проект использует микросервисную архитектуру и включает автоматизированное тестирование, контейнеризацию и возможности облачного развертывания.

### ✨ Основные возможности

#### ✅ Выполнено
- **Система аутентификации пользователей**: регистрация пассажиров/водителей, вход, управление JWT-токенами
- **Система управления поездками**: запрос поездок, отслеживание статуса, отмена, история
- **Система управления водителями**: местоположение водителей, статус доступности, управление заказами
- **Платежная система**: расчет стоимости, обработка платежей, логика возвратов
- **Проверка работоспособности**: мониторинг служб и отчеты о состоянии
- **Полный CI/CD конвейер**: автоматизированное тестирование, сборка, развертывание

#### 🔄 В процессе реализации
- Отслеживание местоположения в реальном времени
- Алгоритм динамического ценообразования
- Система оценок пользователей
- Панель аналитики данных

## 🏗️ Архитектура системы

### Стек технологий
- **Бэкенд-фреймворк**: Flask 2.3.3 (Python 3.11)
- **База данных**: PostgreSQL 15 + SQLAlchemy ORM
- **Кэширование**: Redis 7
- **Контейнеризация**: Docker + Docker Compose
- **Оркестрация развертывания**: Kubernetes
- **CI/CD**: GitHub Actions
- **Документация API**: OpenAPI/Swagger (в планах)
- **Мониторинг**: Prometheus + Grafana (в планах)

### Архитектурная схема
```
Запрос пользователя → Вход Nginx → Кластер Kubernetes → Служба такси → База данных/Кэш
           ↓                    ↓                  ↓                  ↓
        SSL-терминация   Автомасштабирование   Обработка бизнес-логики   Сохранение данных
```

## 📁 Структура проекта

```
taxi-service/
├── .github/workflows/          # Конфигурация конвейера GitHub Actions
├── kubernetes/                 # Конфигурация развертывания K8s (полная производственная среда)
├── src/                        # Исходный код
│   ├── api/                    # Конечные точки REST API
│   ├── models/                 # Модели данных
│   ├── services/               # Сервисы бизнес-логики
│   └── utils/                  # Вспомогательные функции
├── tests/                      # Набор тестов
├── docker/                     # Конфигурация Docker
├── docs/                       # Документация
├── Dockerfile                  # Многоэтапная сборка Docker
├── docker-compose.yml          # Локальная среда разработки
├── requirements.txt            # Зависимости Python
├── run.py                      # Скрипт запуска приложения
└── README.md                   # Этот документ
```

## 🚀 Быстрый старт

### Требования к окружению
- Python 3.11+
- Docker 20.10+
- Docker Compose 2.0+
- Git

### Локальная разработка

#### 1. Клонирование проекта
```bash
git clone https://github.com/ваше-имя-пользователя/taxi-service.git
cd taxi-service
```

#### 2. Настройка окружения Python
```bash
# Создание виртуального окружения
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

#### 3. Настройка переменных окружения
```bash
# Копирование примера конфигурационного файла
cp .env.example .env

# Редактирование файла .env, настройка конфигурации
# Подключение к БД, секретные ключи и т.д.
```

#### 4. Запуск сервера разработки
```bash
# Способ 1: Использование скрипта запуска
python run.py

# Способ 2: Прямой запуск
python src/app.py

# Способ 3: Использование Docker Compose (включает БД и кэш)
docker-compose up --build
```

#### 5. Доступ к приложению
- Корневой адрес API: http://localhost:5000
- Проверка работоспособности: http://localhost:5000/health
- Документация API: http://localhost:5000/docs (в разработке)

### Запуск через Docker
```bash
# Сборка образа
docker build -t taxi-service .

# Запуск контейнера
docker run -p 5000:5000 taxi-service

# Или использование docker-compose (включает все зависимости)
docker-compose up
```

## 🔧 API интерфейсы

### Аутентификация
| Метод | Конечная точка | Описание | Аутентификация |
|------|------|------|------|
| POST | `/api/auth/register` | Регистрация пользователя | Нет |
| POST | `/api/auth/login` | Вход пользователя | Нет |
| POST | `/api/auth/logout` | Выход пользователя | Да |

### Поездки
| Метод | Конечная точка | Описание | Аутентификация |
|------|------|------|------|
| POST | `/api/ride/request` | Запрос поездки | Пассажир |
| GET | `/api/ride/{id}` | Получение деталей поездки | Пассажир/Водитель |
| POST | `/api/ride/{id}/accept` | Принятие заказа водителем | Водитель |
| POST | `/api/ride/{id}/cancel` | Отмена поездки | Пассажир |
| POST | `/api/ride/{id}/complete` | Завершение поездки | Водитель |
| GET | `/api/ride/history` | История поездок | Пассажир/Водитель |

### Водители
| Метод | Конечная точка | Описание | Аутентификация |
|------|------|------|------|
| PUT | `/api/driver/location` | Обновление местоположения | Водитель |
| PUT | `/api/driver/status` | Обновление статуса | Водитель |
| GET | `/api/driver/available` | Доступные водители | Внутреннее |

### Система
| Метод | Конечная точка | Описание | Аутентификация |
|------|------|------|------|
| GET | `/health` | Проверка работоспособности | Нет |
| GET | `/metrics` | Метрики производительности | Нет |

## 🧪 Тестирование

### Запуск набора тестов
```bash
# Установка тестовых зависимостей
pip install pytest pytest-cov

# Запуск всех тестов
pytest

# Запуск тестов с генерацией отчета о покрытии
pytest --cov=src --cov-report=html

# Запуск конкретных тестов
pytest tests/unit/test_auth.py -v
```

### Типы тестов
- **Модульные тесты**: Тестирование отдельных функций и классов
- **Интеграционные тесты**: Тестирование конечных точек API и взаимодействия с БД
- **Сквозное тестирование**: Тестирование полных бизнес-процессов

## 🐳 Развертывание через Docker

### Сборка образа
```bash
# Версия для разработки
docker build -t taxi-service:dev .

# Производственная версия (многоэтапная сборка)
docker build -t taxi-service:prod -f Dockerfile.prod .
```

### Использование Docker Compose
```bash
# Запуск всех служб
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка служб
docker-compose down

# Пересборка служб
docker-compose up --build -d
```

## ☸️ Развертывание в Kubernetes

### Предварительные требования
- Кластер Kubernetes (Minikube, Kind или облачный провайдер)
- Инструмент командной строки kubectl
- Helm (опционально)

### Шаги развертывания

#### 1. Сборка и отправка образа
```bash
cd kubernetes
./build-push.sh
```

#### 2. Развертывание в Kubernetes
```bash
# Полное развертывание (включая БД, кэш, приложение)
./deploy.sh

# Или ручное развертывание
kubectl apply -f namespace.yaml
kubectl apply -f configmap.yaml
kubectl apply -f secrets.yaml
kubectl apply -f database.yaml
kubectl apply -f redis.yaml
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
kubectl apply -f ingress.yaml
kubectl apply -f hpa.yaml
```

#### 3. Доступ к службе
```bash
# Проброс портов (локальное тестирование)
kubectl port-forward svc/taxi-service 8080:80

# Получение внешнего IP (облачная среда)
kubectl get svc taxi-service -o wide

# Просмотр статуса подов
kubectl get pods -l app=taxi-service

# Просмотр логов
kubectl logs -f deployment/taxi-service
```

#### 4. Очистка развертывания
```bash
./cleanup.sh
```

## 🔄 CI/CD конвейер

### Рабочие процессы GitHub Actions
Проект включает полный CI/CD конвейер GitHub Actions:

1. **Проверка качества кода**: Black, Flake8, Pylint
2. **Модульное тестирование**: pytest + отчет о покрытии
3. **Интеграционное тестирование**: Тестирование конечных точек API
4. **Сборка образов**: Мультиархитектурные образы Docker
5. **Отправка образов**: Отправка в GitHub Container Registry
6. **Развертывание**: Опциональное развертывание в Kubernetes

### Условия запуска рабочих процессов
- **Push в ветку main**: Запуск полного CI/CD процесса
- **Pull request**: Запуск проверки кода и тестов
- **Ручной запуск**: Через workflow_dispatch

### Просмотр статуса конвейера
1. Перейдите на вкладку Actions репозитория
2. Просмотрите статус последнего запуска рабочего процесса
3. Нажмите на рабочий процесс для просмотра подробных логов

### Настройка секретов
Настройте следующие секреты в репозитории GitHub для включения полной функциональности:

| Имя секрета | Назначение | Обязательно |
|------------|------|----------|
| `GITHUB_TOKEN` | Доступ к API GitHub | Да (предоставляется автоматически) |
| `KUBECONFIG` | Конфигурация кластера Kubernetes | Нет (если требуется развертывание в K8s) |
| `DOCKER_USERNAME` | Имя пользователя Docker Registry | Нет (если требуется другой реестр) |
| `DOCKER_PASSWORD` | Пароль Docker Registry | Нет (если требуется другой реестр) |

## 📊 Мониторинг и логи

### Мониторинг приложения
- **Конечная точка проверки работоспособности**: `GET /health`
- **Метрики производительности**: `GET /metrics` (формат Prometheus)
- **Структурированные логи**: Формат JSON, поддержка стека ELK

### Мониторинг Kubernetes
```bash
# Просмотр использования ресурсов
kubectl top pods -l app=taxi-service

# Просмотр событий
kubectl get events --sort-by=.metadata.creationTimestamp

# Просмотр статуса HPA
kubectl get hpa taxi-service-hpa

# Просмотр статуса Ingress
kubectl get ingress taxi-ingress
```

## 🔐 Безопасность

### Реализованные меры безопасности
- **Аутентификация JWT**: Аутентификация на основе токенов
- **Хеширование паролей**: Хранение с шифрованием bcrypt
- **Проверка ввода**: Все пользовательские данные проверяются и очищаются
- **Защита от SQL-инъекций**: Использование SQLAlchemy ORM
- **Настройка CORS**: Ограничение межсайтовых запросов
- **Переменные окружения**: Конфиденциальная информация управляется через переменные окружения

### Рекомендации по безопасности
1. Регулярное обновление зависимостей
2. Использование HTTPS для передачи данных
3. Внедрение ограничения скорости API
4. Регулярное сканирование безопасности
5. Ведение журналов аудита

## 📈 Дизайн масштабируемости

### Горизонтальное масштабирование
- **Службы без состояния**: Службы API могут масштабироваться горизонтально
- **Управление сеансами**: Использование Redis для хранения сеансов
- **Пул соединений с БД**: Настройка пула соединений SQLAlchemy

### Вертикальное масштабирование
- **Ограничения ресурсов**: Ограничения CPU/памяти контейнеров
- **Автомасштабирование**: Настройка HPA в Kubernetes
- **Оптимизация БД**: Индексы, оптимизация запросов, разделение чтения/записи

### Планы по расширению
1. **Разделение на микросервисы**: Служба аутентификации, служба поездок, платежная служба
2. **Очередь сообщений**: Асинхронная обработка через RabbitMQ/Kafka
3. **Стратегия кэширования**: Многоуровневое кэширование, интеграция CDN
4. **Шардирование БД**: Шардирование данных на основе геолокации

## 🤝 Руководство по внесению вклада

### Процесс разработки
1. Форкните этот репозиторий
2. Создайте ветку функции: `git checkout -b feature/ваша-функция`
3. Зафиксируйте изменения: `git commit -m 'Добавить некоторую функцию'`
4. Отправьте в ветку: `git push origin feature/ваша-функция`
5. Отправьте Pull Request

### Стандарты кода
- Следование стандартам PEP 8 для кода Python
- Использование Black для форматирования кода
- Добавление подсказок типов (Type Hints)
- Добавление соответствующих строк документации (Docstrings)
- Написание модульных и интеграционных тестов

### Стандарты сообщений о коммитах
Использование стандарта [Conventional Commits](https://www.conventionalcommits.org/):
- `feat:` Новая функция
- `fix:` Исправление ошибки
- `docs:` Обновление документации
- `style:` Форматирование кода (не влияет на функциональность)
- `refactor:` Рефакторинг кода
- `test:` Связано с тестированием
- `chore:` Изменения в процессе сборки или вспомогательных инструментах

## 🐛 Устранение неполадок

### Часто встречающиеся проблемы

#### 1. Ошибка подключения к базе данных
```bash
# Проверка статуса службы БД
docker-compose ps
# Проверка логов БД
docker-compose logs db
# Перезапуск БД
docker-compose restart db
```

#### 2. Ошибка подключения к Redis
```bash
# Проверка службы Redis
docker-compose logs redis
# Тестирование подключения к Redis
docker-compose exec redis redis-cli ping
```

#### 3. Ошибка сборки Docker
```bash
# Очистка кэша Docker
docker system prune -a
# Повторная сборка
docker-compose build --no-cache
```

#### 4. Ошибка развертывания в Kubernetes
```bash
# Просмотр статуса пода
kubectl describe pod <имя-пода>
# Просмотр логов пода
kubectl logs <имя-пода>
# Просмотр событий
kubectl get events
```

### Инструменты отладки
```bash
# Отладка Python
import pdb; pdb.set_trace()

# Отладка Flask
export FLASK_ENV=development
export FLASK_DEBUG=1

# Отладка Docker
docker-compose exec web bash
```

## 📚 Учебные ресурсы

### Связанные технологии
- [Официальная документация Flask](https://flask.palletsprojects.com/)
- [Документация Docker](https://docs.docker.com/)
- [Документация Kubernetes](https://kubernetes.io/docs/)
- [Документация GitHub Actions](https://docs.github.com/en/actions)

### Связанные с проектом
- [Документация API](./docs/api.md)
- [Руководство по развертыванию](./docs/deployment.md)
- [Руководство по разработке](./docs/development.md)
- [Руководство по тестированию](./docs/testing.md)

## 📄 Лицензия

Этот проект использует лицензию MIT - см. файл [LICENSE](LICENSE) для получения подробной информации.

## 🙏 Благодарности

- Сообщество Flask за отличный веб-фреймворк
- Команда Docker за технологии контейнеризации
- Сообщество Kubernetes за решения по оркестрации контейнеров
- GitHub за платформу CI/CD
- Все участники и пользователи

## 📞 Контактная информация

Если у вас есть вопросы или предложения, свяжитесь с нами:

- **GitHub Issues**: [Сообщить о проблеме](https://github.com/wkk08/taxi-service/issues)
- **Электронная почта**:  w08280091@Gmail.com
- **Домашняя страница проекта**: [Репозиторий GitHub](https://github.com/wkk08/taxi-service)

---

**Примечание**: Этот проект разработан в образовательных целях и демонстрирует полный процесс разработки современных веб-приложений и практики DevOps. Развертывание в производственной среде требует корректировки и оптимизации в зависимости от фактической ситуации.

**🚀 Начните свое путешествие в мир служб такси!**